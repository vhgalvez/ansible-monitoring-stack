scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets:
        {% for host in groups['external_node_exporter'] %}
          {% if hostvars[host].ansible_facts.default_ipv4 is defined %}
          - "{{ hostvars[host].ansible_facts.default_ipv4.address }}:9100"
          {% else %}
          - "IP_NO_DISPONIBLE:9100"
          {% endif %}
        {% endfor %}