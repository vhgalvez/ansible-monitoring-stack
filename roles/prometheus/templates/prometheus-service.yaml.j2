apiVersion: v1
kind: Service
metadata:
  name: prometheus-server
  namespace: monitoring
spec:
  ports:
    - port: 9091
      targetPort: 9090
      protocol: TCP
      name: http
  selector:
    app: prometheus
  clusterIP: None